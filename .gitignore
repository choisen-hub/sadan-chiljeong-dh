# Python
__pycache__/
*.pyc

# Source ZIPs (공공데이터포털에서 재다운, 용량 큼)
data/raw/munjip/*.zip

# Derived data (regenerable from 11/12번)
data/raw/letters.jsonl
data/processed/

# data/final/ 은 추적함 (curated deliverables)

# macOS
.DS_Store

# 모델 캐시 (HuggingFace + 자체 캐시)
.cache/

# 엑셀 임시파일 (열려있을 때 생기는 lock file)
~$*.xlsx

# 큰 raw 데이터 (regenerable)
data/raw/
data/intermediate/
transcripts/

# archive 백업 — 큰 임베딩과 대조군 클러스터링은 제외 (재생성 가능)
archive/**/li_token_embeddings.npy
archive/**/clusters/天_clusters.parquet
archive/**/clusters/心_clusters.parquet
archive/**/clusters/性_clusters.parquet
archive/**/clusters/理_clusters.parquet

# archive 백업 — 큰 임베딩과 대조군 클러스터링은 제외 (재생성 가능)
archive/**/li_token_embeddings.npy
archive/**/clusters/天_clusters.parquet
archive/**/clusters/心_clusters.parquet
archive/**/clusters/性_clusters.parquet
archive/**/clusters/理_clusters.parquet
archive/
